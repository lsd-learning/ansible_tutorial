---

- hosts: all
  become: true
  tasks:

  - name: update repository index
    yum:
      update_cache: yes

  - name: Install apache2 package
    yum: 
      name: httpd
      state: latest
      
  - name: add php support for apache
    yum: 
      name: php
      state: latest
